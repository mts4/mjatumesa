// import { useParams } from 'react-router-dom'
// import { useFetchData } from '../../hooks/useFetch'
// import Title from '../../components/Title'
// import SingleProductosSkeletons from '../Skeletons/SingleProductosSkeletons'

const SingleProductoTemplate = () => {
  // const { id } = useParams()

  // const { data, loading, error } = useFetchData(BASE_URL)
  // if (loading) return <SingleProductosSkeletons />
  // if (error) return `Error ${error}`
  // return data.map((character, index) => (
  //   <Title key={index} title={`Nickname ${character.nickname}`} />
  // ))
  return <p>test</p>
}

export default SingleProductoTemplate
