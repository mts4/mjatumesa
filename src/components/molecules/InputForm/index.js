// Styles
import Input from './styles'

const InputForm = ({ placeholder, type, marginRight, marginLeft }) => {
  return (
    <Input
      placeholder={placeholder}
      type={type}
      marginRight={marginRight}
      marginLeft={marginLeft}
    />
  )
}

export default InputForm
